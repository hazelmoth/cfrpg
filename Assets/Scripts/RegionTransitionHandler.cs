using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class RegionTransitionHandler
{
    public static void Transition (Vector2Int worldExitPoint, Direction side)
    {
        throw new NotImplementedException();

        // TODO: Despawn all actors, remove all dropped items, get the appropriate RegionMap,
        // load the map, spawn the player in the correct location, and spawn any actors and
        // dropped items in the new scene.
    }
}
